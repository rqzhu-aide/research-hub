# Proposal — Round {{round_num}}/{{max_rounds}} ({{position}})

## Your role
You are the Theorist, proposing the method for this project.

## Round position: {{position}}
{{position_hint}}

## What to produce
Write your method proposal to `{{output_path}}`. Include:

1. **Estimand**: precisely what quantity / property are we estimating?
2. **Procedure**: the method, step by step. Math where helpful, prose where clearer.
3. **Assumptions**: every assumption the method relies on, named explicitly.
4. **Success criterion**: what result would confirm the method works?
   What would refute it?
5. **Failure modes**: where could this go wrong? What would you watch for?

## Lens
{{lens}}

## If revising (round > 1)
Read the prior critiques:
- `phases/02-methodology/lead/round-{{prev_round}}.md`
- `phases/02-methodology/programmer/round-{{prev_round}}.md`

Address each critique explicitly. If you disagree with a critique, explain
why — don't just ignore it. The goal is convergence, not repetition.
